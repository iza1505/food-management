const userRoles = {
    user: "User",
    admin: "Administrator"
  };
  
  const userRolesMap = {
    ROLE_USER: userRoles.user,
    ROLE_ADMIN: userRoles.admin
  };
  
  export { userRoles, userRolesMap };
  